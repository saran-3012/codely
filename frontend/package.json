{
  "name": "codely-frontend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w studio && npm run build -w admin",
    "dev:studio": "npm run dev -w studio",
    "dev:admin": "npm run dev -w admin"
  }
}
